# Default server configuration (disabled)
# This file is kept for reference but the main configuration is in talki.conf

# server {
#     listen 80 default_server;
#     server_name _;
#     return 444;
# }

