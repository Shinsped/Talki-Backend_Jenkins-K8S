package com.langhakers.talki.entity;

public enum ParticipantRole {
    HOST,           // Session host/moderator
    PARTICIPANT,    // Regular participant
    OBSERVER,       // Read-only observer
    ADMIN,          // Administrative privileges
    GUEST           // Temporary guest access
}
